# Binaries
/cantinode
/cantinode.exe
/dist/
/bin/

# Go
*.test
*.out
/vendor/

# Frontend (dist is embedded via go:embed; only .gitkeep is committed)
/web/node_modules/
/web/dist/*
!/web/dist/.gitkeep

# Runtime data (never commit a live database or config)
*.db
*.db-wal
*.db-shm
config.yaml
/data/

# Personal Claude Code settings
.claude/settings.local.json

# Local reference clones of sibling projects (design/UX comparison only,
# not part of this repo)
/examples/

# Editor / OS noise
.vscode/
.idea/
Thumbs.db
.DS_Store
*.tsbuildinfo
