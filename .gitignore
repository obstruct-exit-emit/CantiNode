# Binaries
/cantinode
/cantinode.exe
/dist/
/bin/

# Old CantiNode source, kept locally for reference during the rewrite
/exmples/

# Go
*.test
*.out
/vendor/

# Frontend (dist is embedded via go:embed; only .gitkeep is committed)
/web/node_modules/
/web/dist/*
!/web/dist/.gitkeep

# Runtime data (never commit a live library DB or config)
*.db
*.db-wal
*.db-shm
config.yaml

# Personal Claude Code settings
.claude/settings.local.json

# Editor / OS noise
.vscode/
.idea/
Thumbs.db
.DS_Store
*.tsbuildinfo
